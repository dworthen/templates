"""Cache Plugin Factory."""

from {{packageName}}.factories.factory import Factory
from {{packageName}}.plugins.cache.cache_plugin import CachePlugin


class CachePluginFactory(Factory[CachePlugin]):
    """Cache Plugin Factory."""

    @property
    def _entry_points_registration_key(self) -> str:
        """Get entry points registration key."""
        return "{{packageName}}.plugins.cache"

    @property
    def _plugin_type(self):
        """Get the type of the plugin."""
        return CachePlugin
